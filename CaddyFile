itknows.io, www.itknows.io {
  encode zstd gzip

  # API to backend
  @api path /health /ask /upload
  reverse_proxy @api backend:8080

  # everything else → static frontend
  reverse_proxy / frontend:80
}